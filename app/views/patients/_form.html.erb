<%= form_with(model: patient, local: true) do |form| %>
  <% if patient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(patient.errors.count, "error") %> prohibited this patient from being saved:</h2>

      <ul>
      <% patient.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :card_id %>
    <%= form.number_field :card_id, id: :patient_card_id %>
  </div>

  <div class="field">
    <%= form.label :registered_on %>
    <%= form.date_select :registered_on, id: :patient_registered_on %>
  </div>

  <div class="field">
    <%= form.label :facility_id %>
    <%= form.text_field :facility_id, id: :patient_facility_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
